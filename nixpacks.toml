[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate", "node scripts/migrate.js", "npm run build"]

[start]
cmd = "npm run start"
